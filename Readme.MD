#DotNet Core Controller Helper

I created this bash file to create controllers and methods via command line.

# Usage

**Create Controller:**

```bash
./dn.sh -c ControlName
```

**Add Method to Controller**

```bash
./dn.sh -am ControlName NewMethodName MethodType
```

